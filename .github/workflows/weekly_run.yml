name: 瘥勗?∟隅?Ｗ???
on:
  schedule:
    # ?啁??瘥曹? 15:00 = UTC 07:00嚗?撘??券??乩???yfinance end嚗??圈曹??嗥鞈?
    - cron: '0 8 * * 5'
    # ?啁??瘥曹? 15:30 ?嚗?撘??典?銝?望??炎?伐??踹???撖?    - cron: '30 8 * * 5'
  workflow_dispatch:          # ?迂???函雯??閫貊

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run-weekly-signal:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: ??蝔?蝣?
        uses: actions/checkout@v4

      - name: 摰? Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 摰?憟辣
        run: |
          pip install yfinance pandas requests playwright google-api-python-client google-auth
          playwright install --with-deps chromium

      - name: ?瑁?瘥梯隅?Ｗ???        env:
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
          GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
          GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.GOOGLE_OAUTH_REFRESH_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ vars.GOOGLE_DRIVE_FOLDER_ID }}
        run: python stock_market_tracking_system.py

